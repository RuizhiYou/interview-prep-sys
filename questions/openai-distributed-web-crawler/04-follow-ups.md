# Follow-up Questions

These prompts were captured from the source requirements chapter. Answers remain open for interview practice.

| Status | Question | Short answer |
| --- | --- | --- |
| `open` | Is the crawl open-domain or limited to a known site set? | — |
| `open` | Do we retain raw HTML, extracted text, or both? | — |
| `open` | How do we detect near-duplicate content across different URLs? | — |
| `open` | How is the one-request-per-domain rule enforced globally? | — |
| `open` | What happens to leased URLs when a worker crashes? | — |

